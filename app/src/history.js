// React-router way of tracking browsing history. Used for navigating back and forth and to programmatically navigate based on certain events
import { createBrowserHistory } from 'history';

export default createBrowserHistory();